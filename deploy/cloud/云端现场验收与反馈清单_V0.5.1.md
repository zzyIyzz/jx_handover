# 江西片区智能交接班 V0.5.1 云端现场验收与反馈清单

> 这份清单用于你在阿里云 ECS 上实际测试后反馈。不要粘贴 `.env`、Qwen Key、初始/个人密码、会话密钥、Cookie 或 OSS 临时凭证。

## 一、部署信息

- [ ] GitHub 仓库已设为 Private，未从公共仓库分发含内部种子数据的源码

| 项目 | 实际值 |
|---|---|
| 测试日期/时间 |  |
| 测试人 |  |
| 阿里云地域 |  |
| ECS 系统与架构 |  |
| vCPU / 内存 |  |
| ESSD 总量/剩余 |  |
| 程序 Git 提交 |  |
| Docker 版本 |  |
| Docker Compose 版本 |  |
| 宝塔版本 / Nginx 版本 |  |
| 正式数据目录（可隐藏上级路径） |  |
| 访问方式：单位固定 IP / VPN |  |
| 域名（可脱敏） |  |

## 二、端口与访问边界

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | `ss -lntp \| grep -E ':(1215\|8765)'` | Nginx 公网监听 1215，Docker 只有 `127.0.0.1:8765` |  |
| 2 | 阿里云安全组 + 宝塔系统防火墙 | 两层都只向单位/VPN来源开放 1215；都没有 8765、数据库端口公网规则 |  |
| 3 | SSH | 只允许管理员固定 IP |  |
| 4 | 宝塔面板 | 已改默认端口，只允许管理员固定 IP |  |
| 5 | 白名单内打开 `https://域名或公网IP:1215` | 正常显示登录页 |  |
| 6 | 手机关闭 Wi-Fi、使用白名单外流量 | 返回 403，不能看到人员名单 |  |
| 7 | `curl -I http://域名或公网IP` | `Location` 明确跳转到 `https://…:1215`；用户收藏夹也显式含 `:1215` |  |
| 8 | HTTPS 证书 | 域名匹配、浏览器无警告 |  |
| 9 | 直接访问 `公网IP:8765` | 无法连接；内部端口未公开 |  |

## 三、容器与配置保护

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | `docker compose config --quiet` | 退出码 0 |  |
| 2 | `docker compose ps` | app 为 running/healthy |  |
| 3 | `/api/health` | `status=ok`、`version=0.5.1`、`mode=cloud`、`login_mode=account`、`port=8765`、`public_port=1215` |  |
| 4 | 故意把 `JX_ACCOUNT_LOGIN_ENABLED` 改为 0 后启动（只在测试环境） | 拒绝启动并说明云端必须使用个人账号 |  |
| 5 | 恢复正确 `.env` | 正常启动 |  |
| 6 | 查看容器 | 单实例、单 worker、只读根文件系统、非 root 用户 |  |
| 7 | 对同一姓名连续输错个人密码 | 达到上限后返回 429，稍后恢复；不存在姓名与错误密码提示相同 |  |
| 8 | 登录 Cookie | Secure、HttpOnly、SameSite=Strict |  |
| 9 | `.env` 权限 | `0600`，未提交 Git |  |

## 四、真实业务全流程

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | 两台电脑分别输入不同人员姓名和初始密码 `aaaa0000*` | 都进入首次改密页，不能提前进入业务页面；登录页不枚举完整人员名单 |  |
| 2 | 两人分别设置至少 12 字符的新密码 | 改密成功，初始密码失效，其他设备旧登录失效 |  |
| 3 | 管理员重置其中一人密码 | 对方立即退出；可用初始密码重新登录并再次强制改密 |  |
| 4 | 两人分别修改不同业务数据 | “最近操作记录”显示准确的操作人、结果、时间和客户端 IP，不含正文或密码 |  |
| 5 | 在测试账号上执行本机应急重置命令 | 只重置准确姓名、旧会话失效、最近操作中有“服务器管理员（本机命令行）”记录 |  |
| 6 | 创建/打开真实班次 | 基本信息自动保存 |  |
| 7 | 第二章新增、编辑、删除 | 可撤销/提示清楚，不丢数据 |  |
| 8 | 用实际《导入模板.xlsx》预览 | 预览不落正式库，错误精确到工作表/行/字段 |  |
| 9 | 确认有效行 | 三、四、五章正确入库 |  |
| 10 | 对同一预览重复确认 | 不重复写入 |  |
| 11 | 人工改章/状态/人员/日期/顺序 | 人工结果优先并保持 |  |
| 12 | 两人同时改同一条 | 后提交者看到冲突提示，不静默覆盖 |  |
| 13 | 第五章无数据 | 网页为空状态；Word 保留三行占位 |  |
| 14 | 生成 Word | 六章和 6.1/6.2 都在，6.3 位于其后 |  |
| 15 | 下载历史 Word | Windows 迁移来的历史记录也能下载 |  |
| 16 | 长文本/中文文件名 | 页面、Excel 导入、Word 均无乱码或截断 |  |

## 五、Qwen3.8-Flash

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | 系统管理“测试 AI 连接” | 成功且模型显示 qwen3.8-flash |  |
| 2 | 实际日志 AI 整理 | 只处理当前场站/时段的相关行 |  |
| 3 | 临时填错 Key 或断开外网（测试后恢复） | 导入不中断，明确提示并回退本地规则 |  |
| 4 | 浏览器开发者工具 | 看不到 Qwen API Key |  |

## 六、本地完整备份与 OSS

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | 网页“立即创建完整备份” | ZIP、JSON、SQLite 校验通过 |  |
| 2 | 运行 `backup-and-sync-oss.sh` | 先 ZIP 后 JSON，命令退出 0 |  |
| 3 | OSS Bucket | 私有、加密、版本控制已开 |  |
| 4 | ECS RAM 角色 | 无长期 AK，仅最小前缀读写权限 |  |
| 5 | 宝塔计划任务 | 每天执行，日志路径可读 |  |
| 6 | `restore-backup-from-oss.sh <编号>` | 下载后全量校验通过 |  |
| 7 | 在测试环境安排恢复并重启 | 恢复成功，自动保留恢复前备份 |  |
| 8 | 恢复后业务核对 | 班次、导入原件、历史 Word 完整 |  |

## 七、重启、升级与故障测试

| 编号 | 检查 | 预期 | 结果/备注 |
|---:|---|---|---|
| 1 | `docker compose restart app` | 60 秒左右恢复 healthy |  |
| 2 | ECS 重启 | Docker 自动启动，应用自动恢复 |  |
| 3 | 容器删除后 `docker compose up -d` | 数据仍在宿主机目录 |  |
| 4 | Qwen 网络故障 | 核心录入和 Word 不受影响 |  |
| 5 | OSS 暂时不可用 | 正式业务不受影响，本地完整备份仍在 |  |
| 6 | 磁盘剩余低于计划阈值 | 有监控告警，不等写满后才发现 |  |
| 7 | 升级前备份、重建镜像 | 数据不被镜像覆盖 |  |

## 八、反馈时附带的安全信息

可以提供：

```bash
cd /www/jx-handover/app/deploy/cloud
docker compose ps
docker compose logs --tail 200 app
curl -sS -H 'Host: 你的域名或公网IP:1215' http://127.0.0.1:8765/api/health
ss -lntp | grep -E ':(1215|8765)'
df -h /www/jx-handover/data
nginx -t
```

必须先删除或遮挡：公网 IP、完整域名（如公司要求）、人员敏感信息、Cookie、Authorization 头、Qwen Key、初始/个人密码、会话密钥、任何 AccessKey/STS Token。

## 九、最终结论

- [ ] 通过，可扩大试用人数
- [ ] 有条件通过，需修复以下问题后复测
- [ ] 不通过，已回到 Windows V0.4.1

问题清单、复现步骤、期望结果：

1. （填写）
2. （填写）
3. （填写）
