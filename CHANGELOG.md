# 更新记录

## V0.3.0 — 2026-08-30

本版本以“更少操作、更清楚反馈、原六章一个不少”为目标，完成完整六章、真实工作日志预览导入、原版式 Word 和 Windows 无控制台 EXE 的整体升级。

### 人性化操作

- 新增 `交接班系统.exe` 图形控制器，固定在 `127.0.0.1:8765` 本机运行。
- 控制器支持启动、打开、停止、重启、打开数据目录、打开日志和复制错误信息。
- 增加“未启动、正在启动、运行中、停止中、启动失败”五种明确状态。
- 增加单实例保护、健康检查、端口归属判断和自动打开浏览器选项记忆。
- 关闭控制器时确认停止；最小化时服务继续运行。
- 端口被其他程序占用时只提示，不强制结束其他进程。

### 完整六章

- 网页和 Word 固定保留一至六章，原章节不改名、不合并、不缺失。
- 第三章“重点工作完成情况”和第四章“需交接的工作”改为独立列表，分别统计、搜索、新增、编辑、排序和批量确认。
- 章节、完成状态和优先级彼此独立；人工改章优先于系统建议。
- 第三章增加“完成人”；第四章保留交接前责任人、交接后责任人和完成情况。
- 第二章增加编辑、删除确认及撤销提示。
- 第五章增加完整外委考核 CRUD 与排序；空数据 Word 仍保留三行占位。
- 原 6.1 月度、6.2 季度始终存在；6.3 年度作为新增小节追加在其后。
- 人员选择器和 Word 仅显示姓名，岗位字段继续保留在接口和数据库。

### Excel 预览导入

- 新增标准模板下载，包含第三章、第四章、第五章和填写说明四个工作表。
- 新增“预览后确认”流程；预览阶段不修改正式数据。
- 错误和警告精确到工作表、行号和字段，部分有效文件可继续提交。
- 同一班次按标准化内容、章节和关键日期识别重复项；提交与重复提交均保持幂等。
- 保存导入原件、SHA256、原始工作表、行号和内容，支持来源追溯。
- 新增可注册解析器，并完成实际工作日志适配：支持合并日期/场站、重点样式、人员、完成状态和多日进度合并。

### 数据、Word 与发布

- 新增幂等 SQLite 迁移：旧事项回填章节、增加排序和完成人字段、新增外委考核与预览表。
- 数据库迁移前自动备份；历史 Word 与 `DocumentSnapshot` 不删除、不重写。
- 正式 EXE 数据统一保存到 `%LOCALAPPDATA%\JXHandover`，程序升级不覆盖数据。
- 支持首次运行自动寻找旧版 `runtime`，也可手工选择旧版数据目录。
- Word 改用经脱敏的原始 DOCX 版式母版，保留标题、说明、页边距、字体、列宽、边框、底色和分页表现。
- Word 生成前增加六章顺序、表头、数据行、占位行、模板残留和 ZIP 完整性校验。
- 发布方式升级为 PyInstaller `onedir + windowed`，不依赖目标电脑安装 Python 或 Node。
- 发布产物增加完整 ZIP、SHA256、标准导入模板和升级说明。

### 验证

- 新增迁移幂等、备份、章节建议与人工改章、颜色/排序独立、第三至第五章 CRUD、冲突保护、预览/提交幂等和实际日志解析测试。
- 新增 Word 六章、表头、行数、排序、颜色、空章节、备注去重与占位行测试。
- 完成原示例、空数据、长文本密集和实际日志导入四组 Word 的逐页渲染检查。
- 完成中文及空格路径、端口冲突、重复启动、停止、重启、升级数据保留和无控制台窗口验收。

## V0.2.0 — 2026-08-28

本版本集中解决“入口分散、重复填写、复核点击多、发布状态不直观”四类问题。

### 新增

- 全新交接班工作台：展示班次、待复核、可生成和已发布数量。
- 页面内 XLSX 拖拽导入：班会记录与定期工作计划无需再调用接口。
- 新建班次智能默认值：最近 10 天、截止日自动作为交接日、记住上次场站。
- 复核进度概览与章节快捷导航。
- 专业事项按待复核、紧急/重点、已完成筛选，并支持关键词搜索。
- 卡片直接确认、单事务批量确认。
- 「保存并确认，下一条」与 `Ctrl + Enter` 快捷键。
- 值班人员选择后自动保存。
- 定期工作一键勾选完成并自动保存。
- Word 生成后自动下载，页面突出显示最新版本。
- 桌面与窄屏响应式布局。
- 高频工作流自动回归测试。

### 修复

- 修复班会记录出现未识别日期时导入过程异常的问题。
- 修复导入结果中的未识别日期统计类型错误。
- 修复同一份定期工作计划重复导入会产生重复行的问题。
- 修复季度 Sheet 会错误影响后续其他 Sheet 分类的问题。
- 增加无效日期、非 XLSX、空文件和超大文件的明确提示。
- 导入存档保留用户原始文件名，便于追溯。

### 接口

- 新增 `POST /api/handover-items/{id}/review`，一次完成保存与确认。
- 新增 `POST /api/handovers/{id}/approve-all`，一次事务确认单场站全部待复核事项。

## V0.1.0 — 2026-08-25

- 完成交接班最小闭环：数据导入、事项整理、人工复核、定期工作、Word 渲染与版本快照。
- 新增内置定期工作模板库和人员字典。
