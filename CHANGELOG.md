# 更新记录

## V0.5.0 — 2026-09-01（阿里云私有云部署测试版）

本版本新增阿里云 ECS + 宝塔 Linux + Docker 的正式测试路线。当前只允许部署在单位固定公网 IP 白名单或企业 VPN 后面；真实 ECS、HTTPS、OSS 和跨平台恢复现场验收完成前，不合并 `main`、不创建稳定标签或正式 Release。

### 云端运行与数据边界

- 新增多阶段 Dockerfile：Node 构建 Vue，Python 运行 FastAPI；容器使用非 root UID 10001、只读根文件系统、最小 Linux capabilities 和日志轮转。
- Compose 固定一个应用实例、一个 Uvicorn worker，并且只将 `8765` 发布到 ECS 宿主机 `127.0.0.1`；公网只开放宝塔 Nginx 的 HTTPS 443。
- 正式 SQLite、导入原件、历史 Word 和快照持久化到可自定义的 ECS 本地 ESSD 目录，镜像重建不覆盖数据；OSS/NAS 仍禁止承载活数据库。
- 新增云端启动前恢复入口，管理员在网页安排恢复后，容器重启会先保留 `pre-restore` 完整备份再切换数据。
- 修复 Windows 完整备份恢复到 Linux 时，历史 Word 绝对路径中的反斜杠无法识别的问题。

### 公网边界加固

- 新增 `cloud` 模式和失败关闭配置检查：必须使用无路径 HTTPS 域名、精确可信 Host、Secure Cookie、至少 12 位访问口令、至少 32 位会话密钥、管理员、24 小时内会话和 `private` 访问声明。
- 新增 Trusted Host、跨来源写请求阻止、登录失败限流、HSTS、CSP、禁止 iframe、MIME 嗅探与 Referrer/Permissions Policy。
- 云端 `/api/health` 只公开版本、模式和数据库可用性；数据根目录、备份状态和服务器身份仍只在管理员诊断接口显示。
- 云端关闭 FastAPI 文档入口；Qwen Key 只从服务器 `.env` 注入，不进入镜像、前端或 Git。
- 明确当前姓名 + 共享口令不能直接承担任意公网身份认证；移除 IP/VPN 白名单前必须升级为个人账号、独立密码和 MFA。

### 宝塔、OSS 与运维

- 新增宝塔 Nginx 反向代理、SSL、IP/VPN 白名单示例和安全组端口表。
- 新增 ECS 主机准备、Compose 校验/构建/启动、固定 SHA256 的 ossutil 安装脚本。
- 新增每日完整备份、ECS RAM 角色上传私有 OSS、从 OSS 下载、全量校验和恢复脚本；不保存长期 AccessKey。
- 新增完整阿里云部署说明、云端现场验收反馈清单和后续身份、高可用、人性化操作优化计划。
- 后端直接依赖锁定版本，Shell 文件强制 LF，降低 Windows 开发后 Linux 容器启动和依赖漂移风险。

### 验证

- 原 32 项后端回归继续通过；新增 6 项云端测试，覆盖失败关闭、示例配置拒绝启动、最小健康信息、可信 Host、跨站写阻止、Secure Cookie、安全响应头、登录限流、Windows→Linux 路径重写和端口发布约束。
- 前端生产构建通过；Compose `config --quiet` 和全部 Bash 脚本 `bash -n` 通过。
- 当前开发机 Docker 客户端可用但 Linux 引擎未运行，因此真实镜像构建、容器健康、宝塔 HTTPS、IP 白名单、OSS RAM 角色和恢复演练仍是阿里云现场发布门槛。

## V0.4.1 — 2026-09-01（生产加固现场测试版）

本版本在 V0.4.0 局域网多人版上补齐备份、恢复、NAS 断线重试、运行诊断和不覆盖旧版的升级准备链路。当前先用于现场测试；在真实服务器完成重启、断网、恢复和回滚演练前，不创建稳定标签或正式 Release。

### 完整备份与 NAS

- 手动和每日备份升级为完整 ZIP，包含 SQLite 一致快照、导入原件、历史 Word 和不含密钥的配置说明。
- 生成后检查 ZIP、清单内每个文件 SHA256 以及 SQLite `quick_check`；校验完成前不会向 NAS 暴露最终文件名。
- NAS 复制失败时保留本地完整备份和“等待重试”状态；恢复连接后可批量补同步。
- 新增由真实服务器进程执行的 NAS 创建、改名、读取、删除测试，并显示实际 Windows 身份。

### 可验证恢复

- 网页新增备份与恢复中心，可列出、重新校验、补同步和安排恢复。
- 恢复任务只在下次安全重启、FastAPI 和 SQLAlchemy 打开数据库之前执行。
- 恢复前自动生成 `pre-restore` 完整备份；数据库、导入原件和历史 Word 作为同一恢复集切换。
- 恢复失败时回放本次启动前的数据，并记录可查看的失败结果；历史 Word 绝对路径在新数据根目录自动修正。
- 控制器新增恢复启动二次确认、长时间进度等待、结果提示、完整备份目录和待恢复取消入口。

### 健康与升级

- 管理员面板显示数据库完整性、本机磁盘空间、访问地址、最近使用端、备份时间、待同步数量和恢复结果。
- 控制器可核对新版 ZIP 与同名 `.sha256`，拒绝路径穿越、损坏包或缺少 EXE 的包，并只解压到独立版本目录。
- 已准备的新旧程序目录并列保留；切换失败时可从旧目录启动，不覆盖程序或业务数据。
- 构建产物新增 `release-info.json` 和外部 `.release.json` 发布清单。
- 后端版本、前端版本、EXE 元数据、部署说明和构建脚本统一为 `0.4.1`，端口仍固定为 `8765`。

### 验证

- 自动化测试增至 31 项，新增完整备份内容、NAS 失败后补同步、恢复前留底、空白替代服务器异机恢复、升级包 SHA/ZIP 安全和旧版目录保留测试。
- 最终打包冒烟脚本增加完整 ZIP 内容、NAS SHA、待恢复重启、`pre-restore` 和恢复后数据状态检查。

## V0.4.0 — 2026-08-31

本版本新增局域网多人协作、Qwen3.8-Flash、安全的服务器本地数据目录、NAS 完成备份和 Windows 服务端发布包。V0.3.0 单机版继续保留为离线应急入口。

### 局域网多人版

- 新增明确的 `desktop` / `server` 两种运行模式；服务端监听 `0.0.0.0:8765`，浏览器使用服务器固定 IP 或内网 DNS 访问。
- SQLite 正式数据库默认位于服务器本地 ProgramData，也可在第一次启动前通过控制器选择其他本地固定盘；启用 WAL、外键、30 秒 busy timeout 和单服务实例，UNC、映射网络盘与 NAS 不能承载活数据库。
- 新增浏览器姓名登录、可选共享访问口令、HttpOnly 签名会话和管理员身份。
- 所有业务和导入 API 在服务端模式下要求身份；修改请求记录操作人、路径、状态、时间和客户端地址，不记录正文或密钥。
- 基本信息加入乐观锁，与事项、设备变更、外委考核和定期工作一起防止多人静默覆盖。
- 页面新增掉线提示、恢复后自动刷新、会话过期提示和冲突后读取最新数据。
- 新增管理员面板：查看 AI 配置状态、测试连接、创建一致数据库备份和查看最近操作审计。

### Qwen3.8-Flash

- 接入阿里云百炼 OpenAI-compatible 接口，模型固定为 `qwen3.8-flash`。
- 使用严格 JSON Schema 和 `enable_thinking=false` 获取结构化结果，记录 token 使用量但不记录 Key。
- 实际工作日志先按班次和场站确定性筛选，再按最多 40 条分批发送最小预览字段；原工作簿不会直接上传给模型。
- AI 不能修改程序识别的日期和人员，只能提出章节、状态、优先级和文字整理建议。
- Qwen 未配置、超时、断网或返回异常时自动回退本地规则，预览和人工确认流程不中断。
- Key 和系统访问口令由服务器控制器使用 Windows DPAPI 机器级加密，并通过 NTFS ACL 限制读取。

### Windows 服务端与 NAS

- 新增无控制台窗口的 `服务器控制器.exe` 和独立后台 `交接班服务器.exe`。
- 控制器支持保存服务器 IP、本地正式数据目录、Qwen Key、访问口令、管理员姓名和 NAS 路径，以及启动、打开、安全停止、重启、日志、错误复制和共享入口生成。
- 新增本机 IPv4 重新检测和部署配置自检；真实验证 IP 归属、本地目录落盘、NAS 创建/改名/读取/删除权限及 8765 端口，并保存不含密钥的 JSON 报告。
- 新增“迁移正式数据目录”：服务器停止后复制到本机空目录，使用 SQLite 在线备份、逐文件校验和完整性检查，全部通过才切换设置，旧目录不删除。
- 后台进程使用全局单实例互斥；端口被其他程序占用时只报告错误，不结束对方进程。
- 安全停止通过带实例标识的原子 `stop.request` 触发 Uvicorn 优雅退出，不按端口或 PID 强杀进程。
- 新增 SYSTEM 开机自启和当前账户登录自启两种互斥计划任务脚本；防火墙仅放行域/专用网络 TCP 8765。
- 新增 SQLite 在线备份、完整性检查、SHA256 清单和 NAS 临时文件校验后原子改名；NAS 失败不影响本地业务。
- 新增共享盘 `.url` 入口生成，普通用户无需从 NAS 运行 EXE。

### 兼容与验证

- 数据库迁移增加 AI 预览状态列和操作审计表；迁移幂等，执行前包含 WAL 数据的一致备份，历史文档不改写。
- 保留完整六章、6.1/6.2/6.3、原版式 Word、第三至第五章操作和 V0.3.0 标准导入模板兼容性。
- Word 长表数据行禁止跨页拆开，跨页自动重复表头，避免负责人姓名或岗位文字被拆成单字残行。
- 用户实际 `导入模板.xlsx` 已完成本地适配验收：识别为连续工作日志，按场站/日期筛选，预览阶段不写正式事项。
- 后端 25 项测试通过，覆盖模式配置、本地数据目录/网络盘拒绝、数据目录安全迁移、WAL、迁移、登录、岗位隐藏、冲突、AI 严格输出与回退、真实日志边界、在线备份和 NAS 校验。
- 前端生产构建和 `vue-tsc --noEmit` 通过；服务端源码完成健康检查、401 保护、姓名登录、HttpOnly Cookie、审计、每日备份和优雅停止冒烟。

## V0.3.0 — 2026-08-30

本版本以“更少操作、更清楚反馈、原六章一个不少”为目标，完成完整六章、真实工作日志预览导入、原版式 Word 和 Windows 无控制台 EXE 的整体升级。

### 人性化操作

- 新增 `交接班系统.exe` 图形控制器，固定在 `127.0.0.1:8765` 本机运行。
- 控制器支持启动、打开、停止、重启、打开数据目录、打开日志和复制错误信息。
- 增加“未启动、正在启动、运行中、停止中、启动失败”五种明确状态。
- 增加单实例保护、健康检查、端口归属判断和自动打开浏览器选项记忆。
- 关闭控制器时确认停止；最小化时服务继续运行。
- 端口被其他程序占用时只提示，不强制结束其他进程。

### 完整六章

- 网页和 Word 固定保留一至六章，原章节不改名、不合并、不缺失。
- 第三章“重点工作完成情况”和第四章“需交接的工作”改为独立列表，分别统计、搜索、新增、编辑、排序和批量确认。
- 章节、完成状态和优先级彼此独立；人工改章优先于系统建议。
- 第三章增加“完成人”；第四章保留交接前责任人、交接后责任人和完成情况。
- 第二章增加编辑、删除确认及撤销提示。
- 第五章增加完整外委考核 CRUD 与排序；空数据 Word 仍保留三行占位。
- 原 6.1 月度、6.2 季度始终存在；6.3 年度作为新增小节追加在其后。
- 人员选择器和 Word 仅显示姓名，岗位字段继续保留在接口和数据库。

### Excel 预览导入

- 新增标准模板下载，包含第三章、第四章、第五章和填写说明四个工作表。
- 新增“预览后确认”流程；预览阶段不修改正式数据。
- 错误和警告精确到工作表、行号和字段，部分有效文件可继续提交。
- 同一班次按标准化内容、章节和关键日期识别重复项；提交与重复提交均保持幂等。
- 保存导入原件、SHA256、原始工作表、行号和内容，支持来源追溯。
- 新增可注册解析器，并完成实际工作日志适配：支持合并日期/场站、重点样式、人员、完成状态和多日进度合并。

### 数据、Word 与发布

- 新增幂等 SQLite 迁移：旧事项回填章节、增加排序和完成人字段、新增外委考核与预览表。
- 数据库迁移前自动备份；历史 Word 与 `DocumentSnapshot` 不删除、不重写。
- 正式 EXE 数据统一保存到 `%LOCALAPPDATA%\JXHandover`，程序升级不覆盖数据。
- 支持首次运行自动寻找旧版 `runtime`，也可手工选择旧版数据目录。
- Word 改用经脱敏的原始 DOCX 版式母版，保留标题、说明、页边距、字体、列宽、边框、底色和分页表现。
- Word 生成前增加六章顺序、表头、数据行、占位行、模板残留和 ZIP 完整性校验。
- 发布方式升级为 PyInstaller `onedir + windowed`，不依赖目标电脑安装 Python 或 Node。
- 发布产物增加完整 ZIP、SHA256、标准导入模板和升级说明。

### 验证

- 新增迁移幂等、备份、章节建议与人工改章、颜色/排序独立、第三至第五章 CRUD、冲突保护、预览/提交幂等和实际日志解析测试。
- 新增 Word 六章、表头、行数、排序、颜色、空章节、备注去重与占位行测试。
- 完成原示例、空数据、长文本密集和实际日志导入四组 Word 的逐页渲染检查。
- 完成中文及空格路径、端口冲突、重复启动、停止、重启、升级数据保留和无控制台窗口验收。

## V0.2.0 — 2026-08-28

本版本集中解决“入口分散、重复填写、复核点击多、发布状态不直观”四类问题。

### 新增

- 全新交接班工作台：展示班次、待复核、可生成和已发布数量。
- 页面内 XLSX 拖拽导入：班会记录与定期工作计划无需再调用接口。
- 新建班次智能默认值：最近 10 天、截止日自动作为交接日、记住上次场站。
- 复核进度概览与章节快捷导航。
- 专业事项按待复核、紧急/重点、已完成筛选，并支持关键词搜索。
- 卡片直接确认、单事务批量确认。
- 「保存并确认，下一条」与 `Ctrl + Enter` 快捷键。
- 值班人员选择后自动保存。
- 定期工作一键勾选完成并自动保存。
- Word 生成后自动下载，页面突出显示最新版本。
- 桌面与窄屏响应式布局。
- 高频工作流自动回归测试。

### 修复

- 修复班会记录出现未识别日期时导入过程异常的问题。
- 修复导入结果中的未识别日期统计类型错误。
- 修复同一份定期工作计划重复导入会产生重复行的问题。
- 修复季度 Sheet 会错误影响后续其他 Sheet 分类的问题。
- 增加无效日期、非 XLSX、空文件和超大文件的明确提示。
- 导入存档保留用户原始文件名，便于追溯。

### 接口

- 新增 `POST /api/handover-items/{id}/review`，一次完成保存与确认。
- 新增 `POST /api/handovers/{id}/approve-all`，一次事务确认单场站全部待复核事项。

## V0.1.0 — 2026-08-25

- 完成交接班最小闭环：数据导入、事项整理、人工复核、定期工作、Word 渲染与版本快照。
- 新增内置定期工作模板库和人员字典。
