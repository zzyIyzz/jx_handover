# 生产化升级路线

当前原则不变：一台常开 Windows 主机运行唯一服务和服务器本地 SQLite；所有人员通过浏览器访问；NAS/共享盘只承担发布分发、完整备份和灾备恢复源，不承载运行中的数据库。

## V0.4.1：数据安全与现场可恢复（当前测试版）

已实现：

- 数据库、导入原件、历史 Word 的完整 ZIP 备份与独立清单。
- ZIP、逐文件 SHA256、SQLite 完整性三重校验。
- NAS 失败待同步队列、恢复后补同步、真实服务身份权限测试。
- 网页健康、备份与恢复中心。
- 重启前安排恢复、恢复前自动留底、失败回放。
- 仅剩 NAS 备份时，可在空白替代服务器离线导入并恢复。
- 新版 ZIP 校验、路径安全检查、独立版本目录和旧版回滚保留。

转为稳定版前必须在真实环境通过：

- Windows 重启后无人登录自动启动。
- 两台及以上电脑并发使用和冲突提示。
- 实际工作日志预览、确认、Word 六章和历史版本下载。
- NAS 正常、断开、恢复、补同步。
- 测试数据完整恢复和空白替代服务器异机恢复。
- 新旧版本切换与回滚。

现场验收未完成前，不移动 `v0.4.0` 标签，不创建 `v0.4.1` 稳定标签或正式 Release。

## V0.5：服务、身份与访问安全

候选方向：

- 正式 Windows Service/安装器，减少对计划任务和交互控制器的依赖。
- 每人独立 PIN/密码和角色，条件允许时接入单位 AD/LDAP。
- 编辑在线状态、草稿恢复和更清楚的多人冲突处理。
- 内网 DNS 固定别名和单位证书 HTTPS。
- Qwen 使用范围、最小化字段、脱敏、用量和失败率治理。
- 自动升级切换器；先备份、切换失败自动回旧版本。

## V0.6：可选数据库平台升级

只有出现以下任一条件时才评估 PostgreSQL 等客户端/服务器数据库：

- 需要两台以上应用服务器或高可用；
- 数据库必须与应用主机分离；
- 并发写入显著超过当前单 Uvicorn/SQLite 模式；
- 单位审计、备份或灾备制度明确要求数据库平台化。

即使升级为 PostgreSQL，也不能把数据库数据目录直接放在普通 SMB 共享盘上；NAS 仍应保存数据库平台导出的备份，而不是活数据文件。

## 暂不优先

- 向量库、复杂 Agent、多模型编排：当前不会直接减少一线人员操作。
- 公网开放 8765：不符合当前身份和传输安全边界。
- 自动发布且跳过人工复核：不符合正式交接班记录要求。
